services:
  birdcam:
    build: .
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      # Optional: create first admin on first run
      # - ADMIN_USER=admin
      # - ADMIN_PASSWORD=changeme
      - SESSION_SECRET=3ed2ac87c444b868d58f4ba0f2b92377e3b3d4f80f3ca6c1e3198405050cb89f
    volumes:
      - birdcam-data:/app/data
    restart: unless-stopped

volumes:
  birdcam-data:
