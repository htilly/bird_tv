# Optional: create first admin on first run (username / password)
# ADMIN_USER=admin
# ADMIN_PASSWORD=changeme

# Server port (default 3000)
# PORT=3000

# Session secret (set in production)
# SESSION_SECRET=your-random-secret
